apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "rea.fullname" . }}
  labels:
    {{- include "rea.labels" . | nindent 4 }}  
  annotations:    
    eks.amazonaws.com/role-arn: {{ .Values.awsRoleArn }}
